<!DOCTYPE html>
<html lang="zh">

<head>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1">
    <title data-i18n="site_title">礼品卡发放系统</title>

    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/combined.css">
    <script>
        // 服务器端传递的语言推荐
        const recommendLang = "<%= locals.recommendLang || 'en' %>";
        const isChineseIP = <%= locals.isChineseIP ? 'true' : 'false' %>;
        const blockChineseIP = <%= locals.blockChineseIP ? 'true' : 'false' %>;
    </script>
    <script src="/socket.io/socket.io.js"></script>
</head>

<body>
    <% if (blockChineseIP && isChineseIP) { %>
        <div class="container">
            <header>
                <h1 data-i18n="site_title">礼品卡发放系统</h1>
            </header>
            <main>
                <div class="blocked-cn-ip-message">
                    <p data-i18n="blocked_cn_ip">显示礼品卡已发放完毕</p>
                </div>
            </main>
        </div>
    <% } else { %>
        <div class="container">
            <header>
                <h1 data-i18n="site_title">礼品卡发放系统</h1>
                <div class="language-selector logout-group">
                    <select id="languageSelect">
                        <option value="zh" data-i18n="lang_zh">中文</option>
                        <option value="en" data-i18n="lang_en">English</option>
                        <option value="ja" data-i18n="lang_ja">日本語</option>
                        <option value="ko" data-i18n="lang_ko">한국어</option>
                    </select>
                    <a href="#" id="logoutLink" class="logout-link" data-i18n="logout">退出</a>
                </div>
            </header>

            <main>
                <%- include('partials/welcome-page') %>
                <%- include('partials/login-page') %>
                <%- include('partials/waiting-page') %>
                <%- include('partials/verification-page') %>
                <%- include('partials/waiting-verification-page') %>
                <%- include('partials/gift-card-page') %>
                <%- include('partials/checkin-page') %>
                <%- include('partials/history-page') %>
            </main>
        </div>

        <script src="js/i18n.js"></script>
        <script src="js/app.js"></script>
    <% } %>
</body>

</html>