<!DOCTYPE html>
<html lang="<%= recommendLang || 'zh' %>">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/v2/css/tailwind.min.css">
  <link rel="stylesheet" href="/v2/css/font-awesome.min.css">
  <style>
    /* 基础样式与其他页面保持一致 */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
      font-family: BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Open Sans", "Helvetica Neue", sans-serif;
    }

    body {
      background-color: #fff;
      color: #333;
      line-height: 1.6;
    }

    /* 礼品卡样式 */
    .gift-card {
      position: relative;
      border-radius: 16px;
      overflow: hidden;
      transition: transform 0.3s ease;
    }
    
    .gift-card:hover {
      transform: scale(1.02);
    }
    
    .gift-card-overlay {
      position: absolute;
      top: 0;
      left: 0;
      right: 0;
      bottom: 0;
      background: linear-gradient(135deg, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0) 70%);
      pointer-events: none;
    }
    
    .card-content {
      position: relative;
      z-index: 10;
    }
    
    /* 步骤指示器 */
    .steps-indicator {
      display: flex;
      justify-content: center;
      margin-bottom: 2rem;
    }
    
    .step {
      width: 8px;
      height: 8px;
      border-radius: 50%;
      background-color: #e8e8ed;
      margin: 0 4px;
    }
    
    .step.active {
      background-color: #0071e3;
    }
    
    /* 按钮样式 */
    .primary-btn {
      background-color: #0071e3;
      color: white;
      transition: all 0.3s ease;
    }
    
    .primary-btn:hover {
      background-color: #0066cc;
      transform: translateY(-2px);
    }
    
    .primary-btn:active {
      transform: translateY(0);
    }

    /* 成功动画 */
    .success-animation {
      animation: successPulse 2s ease-in-out infinite;
    }
    
    @keyframes successPulse {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.05); }
    }

    /* 礼品卡代码显示 */
    .gift-card-code {
      background: linear-gradient(135deg, #007AFF 0%, #5856D6 100%);
      color: white;
      border-radius: 12px;
      padding: 1rem;
      font-family: 'SF Mono', Monaco, 'Consolas', monospace;
      font-size: 1.2rem;
      letter-spacing: 2px;
      text-align: center;
      margin: 1rem 0;
      position: relative;
      overflow: hidden;
    }

    .gift-card-code::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: linear-gradient(45deg, transparent, rgba(255,255,255,0.1), transparent);
      transform: rotate(45deg);
      animation: shine 3s ease-in-out infinite;
    }

    @keyframes shine {
      0%, 100% { transform: translateX(-100%) translateY(-100%) rotate(45deg); }
      50% { transform: translateX(100%) translateY(100%) rotate(45deg); }
    }

    /* 语言选择器 */
    .language-selector {
      position: fixed;
      top: 20px;
      right: 20px;
      z-index: 100;
    }
    
    .language-selector select {
      background: white;
      border: 1px solid #ddd;
      border-radius: 20px;
      padding: 8px 16px;
      font-size: 14px;
      cursor: pointer;
    }

    /* 用户信息隐藏 */
    .hidden-info {
      background: linear-gradient(90deg, #666 25%, transparent 37%, transparent 63%, #666 75%);
      background-size: 4px 1px;
      color: transparent;
    }
  </style>
  <script>
    const recommendLang = "<%= recommendLang || 'zh' %>";
    const isChineseIP = <%= isChineseIP ? 'true' : 'false' %>;
    const blockChineseIP = <%= blockChineseIP ? 'true' : 'false' %>;
  </script>
  <script src="/socket.io/socket.io.js"></script>
  <title data-i18n="success_title">成功 - 礼品卡领取完成</title>
</head>

<body>
  <!-- 语言选择器 -->
  <div class="language-selector">
    <select id="languageSelect">
      <option value="zh" data-i18n="lang_zh">中文</option>
      <option value="en" data-i18n="lang_en">English</option>
      <option value="ja" data-i18n="lang_ja">日本語</option>
      <option value="ko" data-i18n="lang_ko">한국어</option>
    </select>
  </div>

  <!-- 顶部导航 -->
  <header class="fixed top-0 left-0 right-0 bg-white/80 backdrop-blur-sm border-b border-gray-200 z-50">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <button id="back-btn" class="text-gray-800">
          <i class="fa fa-arrow-left"></i> <span data-i18n="back">返回</span>
        </button>
        <div class="flex items-center">
          <img src="/v2/images/logo.png" alt="礼品卡风格图标" class="h-8 w-auto mr-2">
          <span class="text-lg font-semibold" data-i18n="site_name">GiftCard</span>
        </div>
        <div class="w-16"></div> <!-- 占位，保持居中 -->
      </div>
    </div>
  </header>

  <!-- 成功页面内容 -->
  <section class="py-32 bg-white">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <div class="w-16 h-16 bg-green-100 rounded-full flex items-center justify-center mx-auto mb-4 success-animation">
          <i class="fa fa-check text-green-600 text-2xl"></i>
        </div>
        <h2 class="text-3xl font-bold mb-2" data-i18n="claim_success">领取成功！</h2>
        <p class="text-gray-600 max-w-2xl mx-auto" data-i18n="card_added_success">您的礼品卡已成功添加至您的礼品卡账户</p>
      </div>
      
      <!-- 步骤指示器 -->
      <div class="steps-indicator">
        <div class="step active"></div>
        <div class="step active"></div>
        <div class="step active"></div>
      </div>
      
      <div class="max-w-lg mx-auto mt-8">
        <!-- 礼品卡展示 -->
        <div id="gift-card-container">
          <!-- 礼品卡将由JavaScript动态生成 -->
        </div>
        
        <!-- 礼品卡代码显示 -->
        <div class="gift-card-code" id="gift-card-code-display" style="display: none;">
          <div class="flex items-center justify-center mb-2">
            <i class="fa fa-gift mr-2"></i>
            <span data-i18n="gift_card_code">礼品卡代码</span>
          </div>
          <div id="card-code-value" class="text-2xl font-bold">
            Loading...
          </div>
        </div>
        
        <!-- 成功信息 -->
        <div class="bg-gray-100 p-6 rounded-2xl mb-8">
          <h3 class="text-lg font-medium mb-4" data-i18n="gift_card_details">礼品卡详情</h3>
          <div class="space-y-3 text-sm">
            <div class="flex justify-between">
              <span class="text-gray-600" data-i18n="card_amount">礼品卡金额：</span>
              <span class="font-medium" id="card-amount-display">¥0.00</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600" data-i18n="added_to">添加至：</span>
              <span class="font-medium" id="giftcard-id-display"></span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600" data-i18n="category">分类：</span>
              <span class="font-medium" id="card-category-display">通用礼品卡</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600" data-i18n="valid_until">有效期至：</span>
              <span class="font-medium" id="card-expiry-display">长期有效</span>
            </div>
            <div class="flex justify-between">
              <span class="text-gray-600" data-i18n="added_time">添加时间：</span>
              <span class="font-medium" id="added-time"></span>
            </div>
          </div>
        </div>
        
        <!-- 操作按钮 -->
        <div class="flex flex-col sm:flex-row gap-4">
          <button id="daily-checkin-btn" class="primary-btn px-6 py-3 rounded-full hover:bg-blue-600 transition-colors shadow-lg flex items-center justify-center flex-1">
            <i class="fa fa-calendar-check-o mr-2"></i> 
            <span data-i18n="daily_checkin">每日签到</span>
          </button>
          <button id="view-history-btn" class="bg-white border border-gray-200 text-gray-800 px-6 py-3 rounded-full hover:bg-gray-50 transition-colors shadow-lg flex items-center justify-center flex-1">
            <i class="fa fa-history mr-2"></i>
            <span data-i18n="view_history">查看历史</span>
          </button>
        </div>
        
        <!-- 额外操作 -->
        <div class="mt-6 text-center">
          <button id="logout-btn" class="text-gray-500 hover:text-gray-700 text-sm">
            <i class="fa fa-sign-out mr-1"></i>
            <span data-i18n="logout">退出登录</span>
          </button>
        </div>
      </div>
    </div>
  </section>

  <!-- 页脚 -->
  <footer class="bg-gray-100 py-12 border-t border-gray-200 mt-16">
    <div class="container mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center text-sm text-gray-600">
        <p>&copy; 2023 GiftCard. <span data-i18n="all_rights_reserved">保留所有权利。</span></p>
      </div>
    </div>
  </footer>

  <script src="/js/i18n.js?v=<%= Date.now() %>"></script>
  <script src="/v2/js/app.js?v=<%= Date.now() %>"></script>
</body>
</html>